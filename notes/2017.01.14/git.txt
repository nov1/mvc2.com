pwd - где я сейчас
ls - что в каталоге где я нахожусь. флаг "-la" права доступа
mkdir - создание папки
rm - удалить "-r" удалить каталог и все что внутри "-f" не спрашивать  "-rf" два флага вместе
права drwxr-xr-x, "-" пропуск  d - директория
		Владелец	Группа	Остальные
		  rwx 		 r-x 	  r-x 
r-read  4
w-write 2
x-запускать 1
chmod менять права флаг "-r" права на все 
права выставляются с помощью числа cdmod 777 
сумма 7 5 5 будет по нашему cdmod 755 права по умолчанию
touch coздает имя файл
git add --all все файлы в репозитории
git add *.txt все файлы каталога
git rm --cached имя файла  - удаляет из tracked файлов в untracked
git log проверить комиты
wi редактор esc shift+: w q выйти и сохранить комит
git diff - показывает что конкретно было изменено относительно комита до add, с флагом --staged после add 
git checkout возвращает файл к последнему комиту
git commit -a -m "text" добавляет к себе изменения последнего комита
HEAD указатель на последний комит
git  reset HEAD^ index.php ^ на сколько комитов поднятся вверх
git commit --amend -m "text" перезаписывает предыдущий комит
git clone "ссылка" добавить репозиторий
git pull добавить все последние изменения
git config --global user.email имейл
git config --global user.name имя
git config --list вся информация
"shift" + ":" в редакторе wii консоль
q в редакторе wii выход
git merge "имя ветки"  от которой пошло ответвление  слить ветку веток 
git branch -d "имя ветки" удалить ветку
git-scm.com все GUI для гита
удаленная ветка
если 2 человека меняют два файла то последнему кто заливает приходится скачивать а потом заливать свой комит
после гит лог выдает двоеточие нажать q для выхода
если конфликт мердж то правки в одном и том же файле
git commit -a добавляет все изменения
в wii консоли w-"write" q-"quit" подбисать комит, q+! выйти без сохранения
git branch вывести все ветки
git branch "имя ветки" создать ветку
git checkout "имя ветки" переключиться на другую ветку

http://recens.ru/php/resize_and_crop.html функция обрезки
https://ulogin.ru/constructor.php	кнопки соц сетей
404.dp.ua:2082 хостинг login: www404dp pass: LkzCneltynjd! ДляСтудентов! 
sublime plagin ftpSync 	cnrtl+shift+p 

Специалист Борисов 3-уровень 2 первые лекции 2013 год
WHERE IN 
(asdasd or asdsad) AND asdasd